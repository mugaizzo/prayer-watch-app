<resources>
    <!-- Base application theme for Wear OS -->
    <style name="Theme.PrayerWatch" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@color/background_black</item>
    </style>
</resources>
